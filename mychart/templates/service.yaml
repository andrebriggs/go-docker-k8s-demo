apiVersion: v1
kind: Service
metadata:
  name: {{ include "mychart.name" . }}
  labels:
    app: {{ include "mychart.name" . }}
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    name: http
  selector:
    app: {{ include "mychart.name" . }}
